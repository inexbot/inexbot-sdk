# Modbus

## 1. 호스트 컴퓨터설정modbus主站매개변수

**명령어：** `0x7300`

### 매개변수 목록

| 매개변수名 | 유형 | 필수 | 설명 |
|--------|------|------|------|
| masterStation | object | 예 | 主站매개변수，包含RTU매개변수和TCP매개변수 |
| startAddress | bool | 아니오 | 주소偏移；true表示주소不变，false表示주소自动-1 |

#### masterStation 매개변수

| 매개변수名 | 유형 | 필수 | 설명 |
|--------|------|------|------|
| type | string | 예 | 主站유형；取值"TCP","RTU" |
| processNumber | int | 예 | 需要설정的프로세스号；범위[1,9] |
| RTU | object | 아니오 | RTU매개변수 |
| TCP | object | 아니오 | TCP매개변수 |

#### RTU 매개변수

| 매개변수名 | 유형 | 필수 | 설명 |
|--------|------|------|------|
| baudrate | int | 예 | 波特率 |
| checkBit | string | 예 | 校验비트；取值"N","E","O" |
| dataBit | int | 예 | 데이터비트；범위[5,8] |
| port | int | 예 | 포트 번호 |
| slaveId | int | 예 | 从站ID；범위[0,65535] |
| stopBit | int | 예 | 정지비트；범위[0,1] |

#### TCP 매개변수

| 매개변수名 | 유형 | 필수 | 설명 |
|--------|------|------|------|
| IP | string | 예 | IP주소 |
| endian_type | int | 예 | Float大小端；0表示AB CD，1表示CD AB，2表示BA DC，3表示DC BA |
| port | int | 예 | 포트 번호；범위[0,65535] |

### 요청 예시

```json
{
  "masterStation": {
    "RTU": {
      "baudrate": 115200,
      "checkBit": "E",
      "dataBit": 5,
      "port": 2,
      "slaveId": 1,
      "stopBit": 1
    },
    "TCP": {
      "IP": "192.168.1.14",
      "endian_type": 1,
      "port": 503
    },
    "processNumber": 1,
    "type": "TCP"
  },
  "startAddress": false
}
```

---

## 2. 호스트 컴퓨터조회modbus매개변수

**명령어：** `0x7301`

### 매개변수 목록

| 매개변수名 | 유형 | 필수 | 설명 |
|--------|------|------|------|
| processNumber | int | 예 | 需要조회的프로세스号；범위[1,9] |

### 요청 예시

```json
{
  "processNumber": 1
}
```

---

## 3. 컨트롤러回复호스트 컴퓨터modbus매개변수

**명령어：** `0x7302`

### 매개변수 목록

| 매개변수名 | 유형 | 필수 | 설명 |
|--------|------|------|------|
| type | string | 예 | 主站유형；取值"TCP","RTU" |
| RTU | object | 아니오 | RTU매개변수 |
| TCP | object | 아니오 | TCP매개변수 |
| modbus_state | bool | 예 | Modbus연결상태；true表示已연결，false表示未연결 |
| response_time_out | int | 예 | 读写응답时间；단위ms |
| startAddress | bool | 예 | 주소偏移；true表示주소不变，false表示주소自动-1 |

#### RTU 매개변수

| 매개변수名 | 유형 | 필수 | 설명 |
|--------|------|------|------|
| baudrate | int | 예 | 波特率 |
| checkBit | string | 예 | 校验비트；取值"N","E","O" |
| dataBit | int | 예 | 데이터비트；범위[5,8] |
| port | int | 예 | 포트 번호 |
| slaveId | int | 예 | 从站ID；범위[0,65535] |
| stopBit | int | 예 | 정지비트；범위[0,1] |

#### TCP 매개변수

| 매개변수名 | 유형 | 필수 | 설명 |
|--------|------|------|------|
| IP | string | 예 | IP주소 |
| endian_type | int | 예 | Float大小端；0表示AB CD，1表示CD AB，2表示BA DC，3表示DC BA |
| port | int | 예 | 포트 번호；범위[0,65535] |

### 응답 예시

```json
{
  "RTU": {
    "baudrate": 115200,
    "checkBit": "E",
    "dataBit": 5,
    "port": 2,
    "slaveId": 1,
    "stopBit": 1
  },
  "TCP": {
    "IP": "192.168.1.14",
    "endian_type": 1,
    "port": 503
  },
  "modbus_state": false,
  "response_time_out": 100,
  "startAddress": false,
  "type": "TCP"
}
```

---

## 4. 호스트 컴퓨터조회modbus연결상태

**명령어：** `0x7303`

### 요청 예시

```json
{}
```

---

## 5. 컨트롤러回复호스트 컴퓨터modbus연결상태

**명령어：** `0x7304`

### 매개변수 목록

| 매개변수名 | 유형 | 필수 | 설명 |
|--------|------|------|------|
| ModbusConnect | bool | 예 | modbus연결상태；true表示已연결，false表示未연결 |

### 응답 예시

```json
{
  "ModbusConnect": false
}
```

---

## 6. 호스트 컴퓨터설정modbus从站매개변수

**명령어：** `0x7305`

### 매개변수 목록

| 매개변수名 | 유형 | 필수 | 설명 |
|--------|------|------|------|
| type | string | 예 | 통신方式；取值"TCP","RTU" |
| RTU | object | 아니오 | RTU매개변수 |
| TCP | object | 아니오 | TCP매개변수 |
| master-slave | int | 예 | modbus통신的主从모드；0表示主站，1表示从站 |
| scancycle | int | 예 | 扫描주기；범위[8,1000]ms |
| stoprun | int | 예 | 통신연결 해제时조작；0表示不停机，1表示停机 |

#### RTU 매개변수

| 매개변수名 | 유형 | 필수 | 설명 |
|--------|------|------|------|
| baudrate | int | 예 | 波特率 |
| port | int | 예 | 포트 번호 |
| slaveId | int | 예 | 从站ID；범위[0,65535] |

#### TCP 매개변수

| 매개변수名 | 유형 | 필수 | 설명 |
|--------|------|------|------|
| IP | string | 예 | IP주소（기본값） |
| endian_type | int | 예 | Float大小端；0表示AB CD，1表示CD AB，2表示BA DC，3表示DC BA |
| port | int | 예 | 포트 번호；범위[0,65535] |

### 요청 예시

```json
{
  "RTU": {
    "baudrate": 115200,
    "port": 2,
    "slaveId": 1
  },
  "TCP": {
    "IP": "192.168.1.11",
    "endian_type": 0,
    "port": 502
  },
  "master-slave": 1,
  "scancycle": 100,
  "stoprun": 0,
  "type": "TCP"
}
```

---

## 7. 호스트 컴퓨터조회modbus从站매개변수

**명령어：** `0x7306`

### 요청 예시

```json
{}
```

---

## 8. 컨트롤러回复호스트 컴퓨터modbus从站매개변수

**명령어：** `0x7307`

### 매개변수 목록

| 매개변수名 | 유형 | 필수 | 설명 |
|--------|------|------|------|
| type | string | 예 | 통신方式；取值"TCP","RTU" |
| RTU | object | 아니오 | RTU매개변수 |
| TCP | object | 아니오 | TCP매개변수 |
| enable | bool | 예 | 연결 활성화 |
| master-slave | int | 예 | modbus통신的主从모드；0表示主站，1表示从站 |
| scancycle | int | 예 | 扫描주기；범위[8,1000]ms |
| stoprun | int | 예 | 통신연결 해제时조작；0表示不停机，1表示停机 |

#### RTU 매개변수

| 매개변수名 | 유형 | 필수 | 설명 |
|--------|------|------|------|
| baudrate | int | 예 | 波特率 |
| port | int | 예 | 포트 번호 |
| slaveId | int | 예 | 从站ID；범위[0,65535] |

#### TCP 매개변수

| 매개변수名 | 유형 | 필수 | 설명 |
|--------|------|------|------|
| IP | string | 예 | IP주소（기본값） |
| endian_type | int | 예 | Float大小端；0表示AB CD，1表示CD AB，2表示BA DC，3表示DC BA |
| port | int | 예 | 포트 번호；범위[0,65535] |

### 응답 예시

```json
{
  "RTU": {
    "baudrate": 115200,
    "port": 2,
    "slaveId": 1
  },
  "TCP": {
    "IP": "192.168.1.11",
    "endian_type": 0,
    "port": 502
  },
  "enable": false,
  "master-slave": 1,
  "scancycle": 100,
  "stoprun": 0,
  "type": "TCP"
}
```

---

## 9. 호스트 컴퓨터설정modbus从站연결 활성화

**명령어：** `0x7308`

### 매개변수 목록

| 매개변수名 | 유형 | 필수 | 설명 |
|--------|------|------|------|
| enable | bool | 예 | modbus从站연결 활성화 |

### 요청 예시

```json
{
  "enable": true
}
```

---

## 10. 호스트 컴퓨터설정modbus从站하트비트检测활성화

**명령어：** `0x7309`

### 매개변수 목록

| 매개변수名 | 유형 | 필수 | 설명 |
|--------|------|------|------|
| checkheart | bool | 예 | 하트비트检测활성화 |

### 요청 예시

```json
{
  "checkheart": true
}
```

---

## 11. 호스트 컴퓨터조회modbus从站하트비트检测활성화

**명령어：** `0x703A`

### 요청 예시

```json
{}
```

---

## 12. 컨트롤러回复호스트 컴퓨터modbus从站하트비트检测활성화

**명령어：** `0x703B`

### 매개변수 목록

| 매개변수名 | 유형 | 필수 | 설명 |
|--------|------|------|------|
| checkheart | bool | 예 | 하트비트检测활성화 |

### 응답 예시

```json
{
  "checkheart": true
}
```

---

## 13. 호스트 컴퓨터설정modbus프로그램

**명령어：** `0x703C`

### 매개변수 목록

| 매개변수名 | 유형 | 필수 | 설명 |
|--------|------|------|------|
| jobname | string | 예 | 已选프로그램이름 |
| programid | int | 예 | 프로그램序号；범위[1,300] |
| robot | int | 예 | 현재로봇号 |

### 요청 예시

```json
{
  "jobname": "AA",
  "programid": 1,
  "robot": 1
}
```

---

## 14. 호스트 컴퓨터조회modbus프로그램

**명령어：** `0x703D`

### 매개변수 목록

| 매개변수名 | 유형 | 필수 | 설명 |
|--------|------|------|------|
| num | int | 예 | 조회数量；固定为10 |
| robot | int | 예 | 현재로봇号 |
| startprogramid | int | 예 | 조회序열起始프로그램序号；범위[1,30] |

### 요청 예시

```json
{
  "num": 10,
  "robot": 1,
  "startprogramid": 1
}
```

---

## 15. 컨트롤러回复호스트 컴퓨터modbus프로그램

**명령어：** `0x703E`

### 매개변수 목록

| 매개변수名 | 유형 | 필수 | 설명 |
|--------|------|------|------|
| jobnamelist | array | 예 | 프로그램이름열表；길이为10 |
| robot | int | 예 | 현재로봇号 |
| startprogramid | int | 예 | 序열起始프로그램序号；범위[1,30] |
| sum | int | 예 | 선택프로그램总数 |

### 응답 예시

```json
{
  "jobnamelist": ["AA", "", "", "", "", "", "", "", "", ""],
  "robot": 1,
  "startprogramid": 1,
  "sum": 300
}
```
