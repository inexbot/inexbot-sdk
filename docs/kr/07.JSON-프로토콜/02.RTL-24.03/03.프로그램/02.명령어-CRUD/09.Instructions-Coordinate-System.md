# 指令-坐标系类

---

## 1. 切换工具手 (SWITCHTOOL)

**설명:** 切换工具手，0为无工具手

**指令名：** `SWITCHTOOL`

### 매개변수 설명

| 参数名 | 类型 | 取值范围 | 说明 |
|--------|------|----------|------|
| `_variable` | object | - | 变量类型；手填int类型或int类型，全局或局部 |
| `_variable.data` | float | - | 数据值 |
| `_variable.secondvalue` | int | - | 副值 |
| `_variable.value` | int | [0, 999] | 工具手编号 |
| `_variable.varname` | string | - | 변수명 |
| `logout` | bool | - | 주석 처리 여부：true=작업 파일에서 실행되지 않음；false=주석 처리되지 않음, 실행 가능 |
| `type` | int | 48 | 切换工具手在枚举数列type中为48 |
| `userParamInt` | int | - | 2차 개발에서 사용자 정의, 현재 의미 없음 |
| `userParamString` | string | - | 2차 개발에서 사용자 정의, 현재 의미 없음 |

### 예시

```json
{
  "_variable": {
    "data": 0.0,
    "secondvalue": 0,
    "value": 4,
    "varname": "GI009"
  },
  "logout": false,
  "type": 48,
  "userParamInt": 0,
  "userParamString": ""
}
```

---

## 2. 切换用户坐标 (SWITCHUCOORD)

**설명:** 切换用户坐标

**指令名：** `SWITCHUCOORD`

### 매개변수 설명

| 参数名 | 类型 | 取值范围 | 说明 |
|--------|------|----------|------|
| `_variable` | object | - | 变量类型；手填int类型或int类型，全局或局部 |
| `_variable.data` | float | - | 数据值 |
| `_variable.secondvalue` | int | - | 副值 |
| `_variable.value` | int | [0, 999] | 用户坐标编号 |
| `_variable.varname` | string | - | 변수명 |
| `logout` | bool | - | 주석 처리 여부：true=작업 파일에서 실행되지 않음；false=주석 처리되지 않음, 실행 가능 |
| `type` | int | 49 | 切换用户坐标在枚举数列type中为49 |
| `userParamInt` | int | - | 2차 개발에서 사용자 정의, 현재 의미 없음 |
| `userParamString` | string | - | 2차 개발에서 사용자 정의, 현재 의미 없음 |

### 예시

```json
{
  "_variable": {
    "data": 0.0,
    "secondvalue": 1,
    "value": 4,
    "varname": "GI[GI001]"
  },
  "logout": false,
  "type": 49,
  "userParamInt": 0,
  "userParamString": ""
}
```

---

## 3. 用户坐标转换 (USERCOORD_TRANS)

**설명:** 将B、C用户坐标系叠加（×），结果置入A用户坐标系

**指令名：** `USERCOORD_TRANS`

### 매개변수 설명

| 参数名 | 类型 | 取值范围 | 说明 |
|--------|------|----------|------|
| `CoordA` | int | [1, 999] | 用户坐标A；结果存入该用户坐标系，这里예用户坐标系序号 |
| `CoordB` | int | [1, 999] | 用户坐标B |
| `CoordC` | int | [1, 999] | 用户坐标C |
| `logout` | bool | - | 주석 처리 여부：true=작업 파일에서 실행되지 않음；false=주석 처리되지 않음, 실행 가능 |
| `type` | int | 117 | 用户坐标转换在枚举数列type中为117 |
| `userParamInt` | int | - | 2차 개발에서 사용자 정의, 현재 의미 없음 |
| `userParamString` | string | - | 2차 개발에서 사용자 정의, 현재 의미 없음 |

### 예시

```json
{
  "CoordA": 8,
  "CoordB": 18,
  "CoordC": 28,
  "logout": false,
  "type": 117,
  "userParamInt": 0,
  "userParamString": ""
}
```

---

## 4. 切换外部轴 (SWITCHSYNC)

**설명:** 在程序运行中切换当前使用的外部轴

**指令名：** `SWITCHSYNC`

### 매개변수 설명

| 参数名 | 类型 | 取值范围 | 说明 |
|--------|------|----------|------|
| `logout` | bool | - | 주석 처리 여부：true=작업 파일에서 실행되지 않음；false=주석 처리되지 않음, 실행 가능 |
| `syncgroup` | int | [1, 3] | 要切换到的外部轴的组号 |
| `type` | int | 50 | 切换外部轴在枚举数列type中为50 |
| `userParamInt` | int | - | 2차 개발에서 사용자 정의, 현재 의미 없음 |
| `userParamString` | string | - | 2차 개발에서 사용자 정의, 현재 의미 없음 |

### 예시

```json
{
  "logout": false,
  "syncgroup": 2,
  "type": 50,
  "userParamInt": 0,
  "userParamString": ""
}
```

---
