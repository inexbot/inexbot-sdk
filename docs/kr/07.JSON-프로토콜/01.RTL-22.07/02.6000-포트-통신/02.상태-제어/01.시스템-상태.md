# 시스템상태

## 컨트롤러初始化是否完成

### 조회是否完成

**명령어**：0x4305 `CONTROLLER_INIT_FINISH_INQUIRE`

```json
{}
```

### 컨트롤러回复

**명령어**：0x4306 `CONTROLLER_INIT_FINISH_RESPOND`

**매개변수 설명**：

| 매개변수 | 유형 | 설명 |
|------|------|------|
| finishinit | bool | true：完成，false：未完成 |

```json
{
  "finishinit": true
}
```

---

## 컨트롤러IP설정

### 컨트롤러IP설정

**명령어**：0x4301 `CONTROLLER_IP_SET`

**매개변수 설명**：

| 매개변수 | 유형 | 설명 |
|------|------|------|
| name | string | 网卡名称 |
| address | string | IP주소 |
| gateway | string | 网关 |
| dns | string | DNS서버 |

```json
{
  "name": "p1p1",
  "address": "192.168.1.13",
  "gateway": "192.168.1.1",
  "dns": "114.114.114.114"
}
```

### 조회컨트롤러IP

**명령어**：0x4302 `CONTROLLER_IP_INQUIRE`

无매개변수

### 컨트롤러回复

**명령어**：0x4303 `CONTROLLER_IP_RESPOND`

**매개변수 설명**：

| 매개변수 | 유형 | 설명 |
|------|------|------|
| num | int | 网卡数量 |
| network | array | 网卡信息数组 |

```json
{
  "num": 1,
  "network": [
    {
      "name": "p1p1",
      "address": "192.168.1.13",
      "gateway": "192.168.1.1",
      "dns": "114.114.114.114"
    },
    {
      "name": "p2p1",
      "address": "192.168.1.14",
      "gateway": "192.168.1.1",
      "dns": "114.114.114.114"
    }
  ]
}
```

---

## 컨트롤러License

### 获取컨트롤러ID

**명령어**：0x5052 `IDENTIFY_NUMBER_INQUIRE`

### 컨트롤러回复

**명령어**：0x5053 `IDENTIFY_NUMBER_RESPOND`

**매개변수 설명**：

| 매개변수 | 유형 | 설명 |
|------|------|------|
| controllerID | string | 컨트롤러ID |

```json
{
  "controllerID": "4DC9F0249098C82E"
}
```

### 获取컨트롤러剩余使用天数

**명령어**：0x5055 `USE_REST_DAYS_INQUIRE`

### 컨트롤러回复

**명령어**：0x5056 `USE_REST_DAYS_RESPOND`

**매개변수 설명**：

| 매개변수 | 유형 | 설명 |
|------|------|------|
| restdays | int | 剩余天数，-1：无限期使用，0：使用期限已到 |
| year | int | 过期年份 |
| mon | int | 过期月份 |
| day | int | 过期日 |

```json
{
  "restdays": 30,
  "year": 1,
  "mon": 1,
  "day": 1
}
```

### 更新序列号

**명령어**：0x5057 `LICENSE_SERIAL_SET`

**매개변수 설명**：

| 매개변수 | 유형 | 설명 |
|------|------|------|
| license | string | 컨트롤러序列号 |

```json
{
  "license": "ACOCWKBZHSKBEJHI"
}
```

### 컨트롤러回复

**명령어**：0x5059 `LICENSE_SERIAL_RESPOND`

**매개변수 설명**：

| 매개변수 | 유형 | 설명 |
|------|------|------|
| result | string | 密钥更新成功，컨트롤러即将重启 |
| ivalid | string | 无效的密钥 |
| error | string | 存在破解嫌疑，拒绝解锁 |

```json
{
  "result": "success",
  "ivalid": "无效的密钥!",
  "error": "存在破解嫌疑，拒绝解锁!"
}
```

### 조회图片版本

**명령어**：0x505A `IMAGE_VERSION_INQUIRE`（티치 펜던트无此기능）

### 반환조회结果

**명령어**：0x505B `IMAGE_VERSION_RESPOND`

**매개변수 설명**：

| 매개변수 | 유형 | 설명 |
|------|------|------|
| result | bool | 조회是否成功 |
| version | int | 版本号（조회成功时반환） |

```json
{
  "result": true,
  "version": xx
}
```

### 컨트롤러전송（无权使用提示）

**명령어**：0x505C `DISABLE_CONTROLLER`

**매개변수 설명**：

| 매개변수 | 유형 | 설명 |
|------|------|------|
| reason | string | 컨트롤러无权使用提示信息 |

```json
{
  "reason": ""
}
```

### 컨트롤러전송（剩余天数提醒）

**명령어**：0x505F `REST_DAYS_REMIND`

**매개변수 설명**：

| 매개변수 | 유형 | 설명 |
|------|------|------|
| restDays | int | 컨트롤러전송剩余使用天数 |

```json
{
  "restDays": 0
}
```

---

## 版本号获取

티치 펜던트需要获取当前컨트롤러版本号时，전송下面명령：

**명령어**：0x3402 `VERSIONNUM_INQUIRE`

**매개변수 설명**：

| 매개변수 | 유형 | 설명 |
|------|------|------|
| version | string | 版本号内容 |
| servoVersion | string | 伺服版本 |
| ocmVersion | string | 静态库版本 |
| ioVersion | string | IO板版本 |

```json
{
  "version": "v1.0-rc1-67-gf34dae7",
  "servoVersion": xx,
  "ocmVersion": xx,
  "ioVersion": xx
}
```

### 컨트롤러收到조회명령时，전송下面명령

**명령어**：0x3403 `VERSIONNUM_RESPOND`

**매개변수 설명**：

| 매개변수 | 유형 | 설명 |
|------|------|------|
| configFileVersionMismatch | bool | 구성파일是否匹配，false：不匹配；true：匹配 |
| jobFileVersion | string | 작업파일版本 |
| rtlVersion | string | 컨트롤러版本 |
| sysClock | string | 시스템时钟，格式：YYYY.MM.DD HH:MM:SS |
| version | string | 版本号内容 |
| servoVersion | string | 伺服版本（久同컨트롤러 응답空字符串） |
| ocmVersion | string | 静态库版本（久同컨트롤러 응답空字符串） |
| ioVersion | string | IO版本号（久同컨트롤러 응답空字符串） |
| nodka_c1201 | object | C1201컨트롤러专用节点 |

```json
{
  "configFileVersionMismatch": false,
  "jobFileVersion": "5.2.0",
  "rtlVersion": "22.08.0",
  "sysClock": "2023.11.10 13:29:25",
  "version": "v1.0-rc1-67-gf34dae7",
  "servoVersion": "",
  "ocmVersion": "",
  "ioVersion": "",
  "nodka_c1201": {
    "deviceId": "",
    "hw_version": "",
    "fw_version": "",
    "drv_version": ""
  }
}
```

**nodka_c1201 节点매개변수 설명**：

| 매개변수 | 유형 | 설명 |
|------|------|------|
| deviceId | string | 设备ID |
| hw_version | string | 硬件版本号 |
| fw_version | string | 固件版本号 |
| drv_version | string | 驱动版本号 |

---

## 版本核验

### 조회是否存在当前版本的티치 펜던트프로그램

**명령어**：0x3404 `EXIST_TEACHBOX_EQUAL_QTTP_INQUIRE`

**매개변수 설명**：

| 매개변수 | 유형 | 설명 |
|------|------|------|
| MD5 | string | 티치 펜던트프로그램的MD5值 |

```json
{
  "MD5": "xx"
}
```

### 반환조회结果

**명령어**：0x3405 `EXIST_TEACHBOX_EQUAL_QTTP_RESPOND`

如果未조회到，则需要上传当前版本的프로그램。

**매개변수 설명**：

| 매개변수 | 유형 | 설명 |
|------|------|------|
| backuppath | string | 티치 펜던트프로그램路径 |

```json
{
  "backuppath": "xx"
}
```

### 조회是否存在与컨트롤러版本匹配的티치 펜던트프로그램

**명령어**：0x3406 `EXIST_CONTROLLER_MATCHED_QTTP_INQUIRE`

无매개변수

### 반환조회结果

**명령어**：0x3407 `EXIST_CONTROLLER_MATCHED_QTTP_RESPOND`

若结果为是，则弹框让客户选择是否自动升级。

**매개변수 설명**：

| 매개변수 | 유형 | 설명 |
|------|------|------|
| backuppath | string | 티치 펜던트프로그램路径 |

```json
{
  "backuppath": "xx"
}
```

---

## 重启컨트롤러

重启컨트롤러时，티치 펜던트전송：

**명령어**：0x5061 `REBOOT_CONTROLLER`

无매개변수

---

## 恢复出厂설정

恢复出厂설정时，티치 펜던트전송：

**명령어**：0x5064 `RETURN_FACTORY_SET`

**매개변수 설명**：

| 매개변수 | 유형 | 설명 |
|------|------|------|
| clearConfigFile | bool | 清除所有시스템구성파일 |
| clearExtendedFile | bool | 清除所有扩展파일 |

```json
{
  "clearConfigFile": true,
  "clearExtendedFile": true
}
```

---

## 시스템自动备份

### 备份시스템

티치 펜던트전송：

**명령어**：0x5065 `CONTROLLER_BACKUP_INQUIRE`

无매개변수

### 컨트롤러回复

**명령어**：0x5066 `CONTROLLER_BACKUP_RESPOND`

**매개변수 설명**：

| 매개변수 | 유형 | 설명 |
|------|------|------|
| backUpNum | int | 备份数 |
| backUpName | array | 备份시스템的名字，最多十个 |

```json
{
  "backUpNum": 1,
  "backUpName": ["string", "string", "string"]
}
```

### 恢复시스템

티치 펜던트전송：

**명령어**：0x5067 `CONTROLLER_BACKUP_RESTORE`

**매개변수 설명**：

| 매개변수 | 유형 | 설명 |
|------|------|------|
| backUpOneName | string | 需要恢复的시스템名称 |

```json
{
  "backUpOneName": "string"
}
```

### 컨트롤러回复

**명령어**：0x5068 `CONTROLLER_BACKUP_RESTORE_RESPOND`

**매개변수 설명**：

| 매개변수 | 유형 | 설명 |
|------|------|------|
| success | bool | true：시스템恢复成功，false：시스템恢复失败 |

```json
{
  "success": true
}
```

---

## 伺服매개변수

### 通过컨트롤러설정伺服内部매개변수

**명령어**：0x5071 `SERVO_INSIDE_PARM_SET`

**매개변수 설명**：

| 매개변수 | 유형 | 설명 |
|------|------|------|
| robot | int | 로봇号 |
| servoNum | int | 伺服号 |
| 매개변수名 | object | 伺服매개변수名 |

```json
{
  "robot": 1,
  "servoNum": 1,
  "매개변수名": {
    "value": "",
    "unit": "",
    "upLimit": "",
    "lowLimit": ""
  }
}
```

### 티치 펜던트조회

**명령어**：0x5072 `SERVO_INSIDE_PARM_INQUIRE`

**매개변수 설명**：

| 매개변수 | 유형 | 설명 |
|------|------|------|
| robot | int | 로봇号 |
| servoNum | int | 伺服号 |

```json
{
  "robot": 1,
  "servoNum": 1
}
```

### 컨트롤러 응답

**명령어**：0x5073 `SERVO_INSIDE_PARM_RESPOND`

**매개변수 설명**：

| 매개변수 | 유형 | 설명 |
|------|------|------|
| robot | int | 로봇号 |
| servoNum | int | 伺服号 |
| sendNum | int | 전송编号 |
| totalNum | int | 总数 |
| servo | array | 具体伺服매개변수数组 |

```json
{
  "robot": 1,
  "servoNum": 1,
  "sendNum": 1,
  "totalNum": 1,
  "servo": [
    {
      "name": "电机额定功率",
      "value": 3000
    },
    {
      "name": "电机额定转速",
      "value": 4000
    }
  ]
}
```

---

## 파일传输

### 요청上传某파일

**명령어**：0x5501 `REQUEST_UPLOAD_FILE`

**매개변수 설명**：

| 매개변수 | 유형 | 설명 |
|------|------|------|
| name | string | 파일名称 |
| size | int | 파일大小 |

```json
{
  "name": "vxWorks",
  "size": 4096
}
```

### 同意上传요청/拒绝上传요청

**명령어**：0x5502 `REQUEST_UPLOAD_RESPOND`

**매개변수 설명**：

| 매개변수 | 유형 | 설명 |
|------|------|------|
| answer | string | "yes"：同意上传，"busy"：컨트롤러忙碌 |

```json
{
  "answer": "yes"
}
```

### 요청下载某파일

**명령어**：0x5504 `REQUEST_DOWNLOAD_FILE`

**매개변수 설명**：

| 매개변수 | 유형 | 설명 |
|------|------|------|
| name | string | 파일名称 |

```json
{
  "name": "log.0"
}
```

### 同意下载요청/拒绝下载요청

**명령어**：0x5505 `REQUEST_DOWNLOAD_RESPOND`

**매개변수 설명**：

| 매개변수 | 유형 | 설명 |
|------|------|------|
| answer | string | "yes"：同意下载，"busy"：컨트롤러忙碌，"nofile"：无此파일 |
| name | string | 작업파일全路径 |
| size | int | 如果拒绝，则size无效 |

```json
{
  "answer": "yes",
  "name": "log.0",
  "size": 4096
}
```

---

## 파일传输网络异常中断（仅티치 펜던트）

**명령어**：0x5525 `DownLoadInterrupt`

无매개변수

```json
{}
```

---

## 티치 펜던트조회컨트롤러구성파일目录

**명령어**：0x5507 `CONFIG_FILE_INQUIRY`

**매개변수 설명**：

| 매개변수 | 유형 | 설명 |
|------|------|------|
| isExport | bool | true：表示导出구성，false表示导入구성 |

```json
{
  "isExport": true
}
```

---

## 컨트롤러向티치 펜던트전송구성파일目录

**명령어**：0x5508 `CONFIG_FILE_RESPOND`

**매개변수 설명**：

| 매개변수 | 유형 | 설명 |
|------|------|------|
| filenum | int | 구성파일数量 |
| filelist | array | 구성파일名称列表 |

```json
{
  "filenum": 2,
  "filelist": ["xxx.json", "yyy.json"]
}
```

---

## 전송파일二进制데이터

**명령어**：0x5511 `UploadFileData`（告诉컨트롤러파일전송完成）

**명령어**：0x5512 `UploadFileDone`（循环给컨트롤러전송파일）

### 컨트롤러回复

**명령어**：0x5513 `ReceiveUploadFinish`

**매개변수 설명**：

| 매개변수 | 유형 | 설명 |
|------|------|------|
| finish | bool | true表示上传成功，false表示失败 |

```json
{
  "finish": true
}
```

---

## 티치 펜던트요청获取日志파일列表

**명령어**：0x5542 `LOGFILE_LIST_INQUIRE`

**매개변수 설명**：

| 매개변수 | 유형 | 설명 |
|------|------|------|
| num | int | 表示获取最近多少个파일，可选值：5、30、100 |

```json
{
  "num": 5
}
```

### 컨트롤러回复日志파일列表

**명령어**：0x5543 `LOGFILE_LIST_RESPOND`

**매개변수 설명**：

| 매개변수 | 유형 | 설명 |
|------|------|------|
| absolutepath | string | 日志所在目录 |
| logfilenum | int | 注意，这个数和上面的num不一定相等 |
| logfilelist | array | 日志列表 |

```json
{
  "absolutepath": "log/",
  "logfilenum": 3,
  "logfilelist": ["logInfo.0", "logInfo.1", "logInfo.2"]
}
```

---

## ENI파일列表조회

**명령어**：0x5545 `ENIFILE_LIST_INQUIRE`

**매개변수 설명**：

| 매개변수 | 유형 | 설명 |
|------|------|------|
| num | int | 表示获取最近多少个파일，可选值：5、30、100 |

```json
{
  "num": 5
}
```

### 컨트롤러回复ENI파일列表

**명령어**：0x5546 `ENIFILE_LIST_RESPOND`

**매개변수 설명**：

| 매개변수 | 유형 | 설명 |
|------|------|------|
| absolutepath | string | 파일所在目录 |
| ENIfilenum | int | 注意，这个数和上面的num不一定相等 |

```json
{
  "absolutepath": "log/",
  "ENIfilenum": 3
}
```

---

## 티치 펜던트전송表示已接受完成

**명령어**：0x5544 `DownLoadControlLogFileFinish`

---

## 티치 펜던트요청获取작업파일列表

**명령어**：0x5532 `JOBFILE_LIST_INQUIRE`

### 컨트롤러回复작업파일目录及数量

**명령어**：0x5533 `JOBFILE_SUM_RESPOND`

**매개변수 설명**：

| 매개변수 | 유형 | 설명 |
|------|------|------|
| absolutepath | array | 작업파일路径 |
| jobfilenum | array | 各작업파일路径下的작업파일数量 |

```json
{
  "absolutepath": ["/job/R1/", "/job/R2/", "/job/R3/", "/job/R4/"],
  "jobfilenum": [2, 33, 233, 666]
}
```

### 컨트롤러回复작업파일列表

**명령어**：0x5534 `JOBFILE_LIST_RESPOND`

**매개변수 설명**：

| 매개변수 | 유형 | 설명 |
|------|------|------|
| robot | int | 로봇号 |
| listnum | int | 最多10个 |
| jobfilelist | array | 작업파일列表 |

```json
{
  "robot": 1,
  "listnum": 2,
  "jobfilelist": [
    {
      "name": "xxx.JBR",
      "MD5": "123"
    },
    {
      "name": "yyy.JBR",
      "MD5": "132"
    }
  ]
}
```

### 接受完成전송给示教盒

**명령어**：0x5535 `JOBFILE_LIST_FINISH`

无매개변수。当四个로봇전송完毕后전송给示教盒

---

## 티치 펜던트조회Lua脚本列表

**명령어**：0x5552 `LUASCRIPT_LIST_INQUIRE`

无매개변수

### 컨트롤러回复Lua脚本列表

**명령어**：0x5553 `LUASCRIPT_LIST_RESPOND`

**매개변수 설명**：

| 매개변수 | 유형 | 설명 |
|------|------|------|
| absolutepath | string | Lua脚本所在目录 |
| sum | int | 列表总数 |
| scriptlist | array | 脚本列表 |

```json
{
  "absolutepath": "lua/",
  "sum": 3,
  "scriptlist": ["ww.lua", "ee.lua"]
}
```

---

## 信息提示

### 错误提示

**명령어**：0x2B03 `ERROR_CODE`

### 警告提示

**명령어**：0x2B04 `WARNING_CODE`

### 消息条提示

**명령어**：0x2B05 `INFO_CODE`

**매개변수 설명**：

| 매개변수 | 유형 | 설명 |
|------|------|------|
| data | string | 消息条内容 |
| type | int | 消息代码 |

```json
{
  "data": "예시",
  "type": 1234
}
```

### 弹窗提示

**명령어**：0x2B06 `POPUP_CODE`

**매개변수 설명**：

| 매개변수 | 유형 | 설명 |
|------|------|------|
| count | int | 弹窗按钮数量，最大为3 |
| text1 | string | 弹窗居中显示的主文本 |
| text2 | string | 按钮1的文本 |
| text3 | string | 按钮2的文本 |
| text4 | string | 按钮3的文本 |
| kind | int | 弹窗提示种类，1为消息，2为警告，3为报错 |
| node | string | 로봇序号1-4 |
| Tips | bool | 弹窗呼出为true |

```json
{
  "count": 1,
  "text1": "예시",
  "text2": "예시",
  "text3": "예시",
  "text4": "예시",
  "kind": 1,
  "node": "1",
  "Tips": true
}
```

### 弹窗交互反馈

根据사용자对弹窗的조작进行相应설정：

**명령어**：0x2B07 `GET_OPTION`

**매개변수 설명**：

| 매개변수 | 유형 | 설명 |
|------|------|------|
| node | string | 로봇序号1-4 |
| option | int | 1-3，对应弹窗选项1-3的内容 |

```json
{
  "node": "1",
  "option": 1
}
```

---

## 컨트롤러时间조회

### 时间조회

**명령어**：0x5742 `CONTROLLER_TIME_INQUIRE`

**매개변수 설명**：

| 매개변수 | 유형 | 설명 |
|------|------|------|
| type | int | 1：获取시스템开始时间至现在秒数，0：显示时间 |
| format | int | 1：获取北京时间，0：获取时间戳 |

```json
{
  "type": 0,
  "format": 0
}
```

### 반환조회结果

**명령어**：0x5743 `CONTROLLER_TIME_RESPOND`

**매개변수 설명**：

| 매개변수 | 유형 | 설명 |
|------|------|------|
| tType | int | 1：获取시스템开始时间至现在秒数，0：显示时间 |
| format | int | 1：获取北京时间，0：获取时间戳 |
| date | object | format为1时用格林威治标准时间表示 |
| year | int | 日期中的年份 |
| mon | int | 日期中的月份 |
| day | int | 日期中的天 |
| hour | int | 时间中的小时 |
| min | int | 时间中的分钟 |
| sec | int | 时间中的秒数 |
| msec | int | 时间中的毫秒数 |
| timestamp | object | 以秒和纳秒的形式记录时间戳 |
| s | int | 时间戳中的秒数 |
| ns | int | 时间戳中的纳秒数 |

```json
{
  "tType": 0,
  "format": 0,
  "date": {
    "year": 2023,
    "mon": 1,
    "day": 1,
    "hour": 1,
    "min": 1,
    "sec": 1,
    "msec": 1
  },
  "timestamp": {
    "s": 1,
    "ns": 1
  }
}
```

---

## 复制로봇매개변수

**명령어**：0x9111 `COPY_ROBOT_PARAM`

**매개변수 설명**：

| 매개변수 | 유형 | 설명 |
|------|------|------|
| mainRobot | int | 被复制매개변수的로봇的로봇号 |
| copyToRobot | array | 数组下标为로봇几；数组元素：1：需要复制，0：无需复制 |

```json
{
  "mainRobot": 1,
  "copyToRobot": [0, 1, 0, 1]
}
```

---

## 수신和전송信息

// 0x95XX被호스트 컴퓨터服务기능占有

### 서버전송错误

**명령어**：0x6010 `SEVERO_ERROR`

### 전송로봇错误

**명령어**：0x6020 `ROBOT_ERROR`

### 전송컨트롤러错误

**명령어**：0x6030 `CONTROLLER_ERROR`

### 전송内存错误

**명령어**：0x6040 `MEMORY_ERROR`

### 警告유형

**명령어**：0x6110 `WARNING_TYPE`

### 消息유형

**명령어**：0x6210 `INFO_TYPE`

---

## 硬盘상태

### 硬件상태조회

**명령어**：0x50D2 `HARDDISK_STATUS_INQUIRE`

无매개변수

```json
{}
```

### 硬件상태조회后컨트롤러 응답

**명령어**：0x50D3 `HARDDISK_STATUS_RESPOND`

**매개변수 설명**：

| 매개변수 | 유형 | 설명 |
|------|------|------|
| Estimated_Available_Time | int | 预计可用时间，单位h |
| get_harddisk_warning_time | int | 硬盘预警时间，单位h |

```json
{
  "Estimated_Available_Time": 200,
  "get_harddisk_warning_time": 500
}
```

### 설정硬盘预警时间

**명령어**：0x50D4 `HARDDISK_WARNING_TIME_SET`

**매개변수 설명**：

| 매개변수 | 유형 | 설명 |
|------|------|------|
| set_harddisk_warning_time | int | 硬盘预警时间，单位h |

```json
{
  "set_harddisk_warning_time": 500
}
```

---

## 조작매개변수

### 조작매개변수설정

**명령어**：0x50A1 `OPERATE_PARAM_SET`

**매개변수 설명**：

| 매개변수 | 유형 | 설명 |
|------|------|------|
| OutageKeepFunctionFlag | bool | 断电保持기능 |
| checkServoStatusWordMaxTime | int | 伺服응답时间，单位ms，범위1-1000 |
| runModeAutoManBegFlag | bool | 切到실행 모드自动上电 |
| remoteUseReserveModeFlag | bool | 预约모드 |
| remoteAllowContinueRunFlag | bool | 远程IO断点执行 |
| remoteAllowCurrentRunFlag | bool | 远程IO当前行执行 |
| remoteSwitchModeEnable | bool | 无티치 펜던트切원격 모드 |
| allowReserveRunningPro | bool | 远程IO프로그램실행中再次预约 |
| SetSafeScreenTeachInvalidFlag | bool | 示티치 모드安全光幕不生效 |
| keepStepRunFlag | int | 单步/回零/홈 위치/실행조작方式，0：单击실행，1：按下실행 |
| defaultRunningSpeed | int | 실행 모드开机默认속도 |
| language | string | 语言选项："chinese"、"english"、"korean" |
| craftSelect | string | 프로세스选择：general-通用프로세스，special-专用프로세스，punch-스탬핑프로세스，pallet-팔레타이징프로세스，weld-용접프로세스，cut-切割프로세스 |
| switch_only_note | bool | 명령어插入只显示点位注释，true只显示注释，false都显示 |

```json
{
  "OutageKeepFunctionFlag": false,
  "checkServoStatusWordMaxTime": 100,
  "runModeAutoManBegFlag": false,
  "remoteUseReserveModeFlag": false,
  "remoteAllowContinueRunFlag": true,
  "remoteAllowCurrentRunFlag": false,
  "remoteSwitchModeEnable": false,
  "allowReserveRunningPro": false,
  "SetSafeScreenTeachInvalidFlag": false,
  "keepStepRunFlag": 0,
  "defaultRunningSpeed": 1,
  "language": "chinese",
  "craftSelect": "general",
  "switch_only_note": false
}
```

### 조회

**명령어**：0x50A2 `OPERATE_PARAM_INQUIRE`

无매개변수

### 컨트롤러 응답

**명령어**：0x50A3 `OPERATE_PARAM_RESPOND`

**매개변수 설명**：

| 매개변수 | 유형 | 설명 |
|------|------|------|
| rebootRunEnable | bool | 断电保持기능 |
| defaultRunningSpeed | int | 실행 모드开机默认속도 |
| runModeAutoManBegFlag | bool | 切到실행 모드自动上电 |
| remoteUseReserveModeFlag | bool | 预约모드 |
| remoteAllowContinueRunFlag | bool | 远程IO断点执行 |
| remoteAllowCurrentRunFlag | bool | 远程IO当前行执行 |
| remoteSwitchModeEnable | bool | 无티치 펜던트切원격 모드 |
| allowReserveRunningPro | bool | 远程IO프로그램실행中再次预约 |
| SetSafeScreenTeachInvalidFlag | bool | 示티치 모드安全光幕不生效 |
| keepStepRunFlag | int | 单步/回零/홈 위치/실행조작方式，0：单击실행，1：按下실행 |
| craftSelect | string | 프로세스选择 |
| switch_only_note | bool | 명령어插入只显示点位注释 |

```json
{
  "rebootRunEnable": false,
  "defaultRunningSpeed": 1,
  "runModeAutoManBegFlag": false,
  "remoteUseReserveModeFlag": false,
  "remoteAllowContinueRunFlag": true,
  "remoteAllowCurrentRunFlag": false,
  "remoteSwitchModeEnable": false,
  "allowReserveRunningPro": false,
  "SetSafeScreenTeachInvalidFlag": false,
  "keepStepRunFlag": 0,
  "craftSelect": "general",
  "switch_only_note": false
}
```

---

## 切换시스템

**명령어**：0x5101 `SWITCH_MASTER`

无매개변수

---

## 获取支持的语言

### 조회语言支持

**명령어**：0x50A5 `LANGUAGE_SUPPORT_INQUIRE`

无매개변수

```json
{}
```

### 반환조회结果

**명령어**：0x50A6 `LANGUAGE_SUPPORT_RESPOND`

**매개변수 설명**：

| 매개변수 | 유형 | 설명 |
|------|------|------|
| language | array | 支持的语言列表 |
| language_used | string | 当前使用的语言 |

```json
{
  "language": ["Chinese", "Korean", "English"],
  "language_used": "Chinese"
}
```

### 比对티치 펜던트和컨트롤러当前语言

**명령어**：0x50A7 `TEACHBOX_LANGUAGE`

**매개변수 설명**：

| 매개변수 | 유형 | 설명 |
|------|------|------|
| teachbox_language | string | 티치 펜던트当前语言 |

```json
{
  "teachbox_language": "Chinese"
}
```

---

## 拔出示教盒

### 拔出티치 펜던트전송

**명령어**：0x7277 `TEACHBOXSTATE_CLOSE`

无매개변수

```json
{}
```

### 插入示教盒恢复通信

**명령어**：0x7278 `TEACHBOXSTATE_OPEN`

无매개변수

```json
{}
```
