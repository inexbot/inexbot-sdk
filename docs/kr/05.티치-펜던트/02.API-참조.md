---
title: 2차 개발 API
description: 티치 펜던트2차 개발静态库인터페이스설명，포함 nextp.h、json/json.h、digitallineedit.h、lineeditwidget.h 头파일인터페이스及 Demo 설명。
---

# 2차 개발 API

## 2차 개발静态库소개

### 기능介绍

제공完整的티치 펜던트기능，지원添加사용자自定义인터페이스，지원特定필드的通信与제어시스템通信。

### 静态库目录结构

![티치 펜던트 SDK 静态库目录结构图，展示 nextpLib 下的 Include 头파일和 Library 平台库파일](assets/api-01.png)

nextpLib 为总파일夹；

Include 파일夹中为头파일；

Library 파일夹中为静态库파일包括 linux 平台和 ARM 平台的库파일（사용ARM 平台交叉编译的프로그램只适用于纳博特公司的 T30 티치 펜던트사용）；

### 静态库结构설명

#### 1.nextp.h 头파일인터페이스설명：

```cpp
//创建 Nextp 类对象
static QPointer<Nextp> getInstance();
//获取시스템字体
QString getSystemFont();
//사용자自定义窗体传输到主프로그램
void setWidgetParentLocation(QPointer <QWidget> widget);
//向컨트롤러전송消息
void sendMessage(const quint16 &command,const QByteArray &data);
//通知示教프로그램自定义窗体已经打开
widgetShowFinish()
//수신컨트롤러消息신호
void signal_receiveMessage(const quint16 &command,const QByteArray &data);
//打开自定义窗体신호
void signal_openWidget();
//关闭自定义窗体신호
void signal_closeWidget();
//隐藏프로세스主인터페이스中除了自定义버튼的其他버튼
void hideTechnologyToolbuttons();
//静态库지원与컨트롤러通信명령字
 enum CommandList
 {
    SetFirstUserParaCommand = 0x9200,
    GetFirstUserCommand = 0x9201,
    ReceivedFirstUserCommand = 0x9202,
    SetSecondUserParaCommand = 0x9203,
    GetSecondUserCommand = 0x9204,
    ReceivedSecondUserCommand = 0x9205,
    SetThirdUserParaCommand = 0x9206,
    GetThirdUserCommand = 0x9207,
    ReceivedThirdUserCommand = 0x92,
    ReceivedThirdUserCommand = 0x9208,
    SetFourthUserParaCommand = 0x9209,
    GetFourthUserCommand = 0x920a,
    ReceivedFourthUserCommand = 0x920b,
    SetFifthUserParaCommand = 0x920c,
    GetFifthUserCommand = 0x920d,
    ReceivedFifthUserCommand = 0x920e,
};
```

#### 2.json/json.h 头파일제공 JSON 데이터格式的组装和解析

组装 json 데이터예시:

```cpp
Json::Value root;
Json::FastWriter wt;
root["robot"] =1;
root["booldata"] =true;
root["data"] = 1.1;
root["name"] ="nihao";
```

解析 Json 데이터예시

```cpp
QByteArray jsonData //컨트롤러전송的데이터
Json::Value root;
Json::Reader reader;
QString jsonData = param.data();
if(reader.parse(jsonData.toStdString(), root))
{
    int robot = root["robot"].asInt();
    bool booldata= root["booldata"].asBool();
    Int data= root["data"].asDouble();
    Std::string name = root["name "].asString();
}
```

#### 3.digitallineedit.h 제공数字输入框

지원将 QLineEdit 控件提升为数字输入框

提升方法：右键选择一个 QLineEdit 控件 --->Promote to--->DigitalLineEdit

![Qt Designer 中通过 Promote to 菜单将 QLineEdit 提升为 DigitalLineEdit 的조작](assets/api-02.png)

右侧树形结构中可以看到该控件 Classs 속성变为 DigitLineEdit

![Qt Designer 对象树中显示 lineEdit 的自定义类为 DigitalLineEdit](assets/api-03.png)

프로그램실행后控件效果，单击控件会弹出数字键盘：

![티치 펜던트数字输入框弹出的数字键盘](assets/api-04.png)

#### 4.lineeditwidget.h 제공数字和字符输入框 지원将 QLineEdit 控件提升为数字与字符输入框 提升方法：右键选择一个 QLineEdit 控 件 --->Promote to--->lineEditWidget

![Qt Designer 中通过 Promote to 菜单将 QLineEdit 提升为 LineEditWidget 的조작](assets/api-05.png)

右侧树形结构中可以看到该控件 Classs 속성变为 lineEditWidget

![Qt Designer 对象树中显示 lineEdit_2 的自定义类为 LineEditWidget](assets/api-06.png)

프로그램실행后控件效果，单击控件会弹出数字与字母键盘：

![티치 펜던트字符输入框弹出的数字与字母虚拟键盘](assets/api-07.png)

## Demo 설명

![NextpMode Demo 项目结构及 main.cpp 启动代码示意图](assets/api-08.png)

- Demo 结构图 demo 파일夹名称：NextpMode

2.1 settingparawidget.h settingparawidget.cpp settingparawidget.ui 三个파일为사용자自定义窗体

![티치 펜던트 Demo 的매개변수구성인터페이스，포함四个매개변수输入框和반환、修改、下一页버튼](assets/api-09.png)

2.2 widgetmanager.h widgetmanager.cpp 为管理类연결사용자自定义窗体和静态库

2.2 静态库파일夹 nextplib 필요放置在 demod 的 NextpMode 파일夹下

2.3 실행 Demo （사용 QtCreator 直接打开 NextpMode 파일夹下的 NextpMode.pro 파일 ）

运 Demo 프로그램点击【조작员】> 选择管理员>输入密码 123456 登录

![티치 펜던트管理员登录인터페이스，显示사용자选择、密码输入框和登录버튼](assets/api-10.png)

点击左侧【프로세스】버튼> 사용자 进入自定义窗体

![티치 펜던트프로세스菜单인터페이스，红色方框高亮标注사용자图标](assets/api-11.png)

![티치 펜던트自定义프로세스매개변수页面，포함四个매개변수输入框和조작버튼](assets/api-12.png)

点击修改버튼 可以修改매개변수 点击保存将전송매개변수到컨트롤러

![티치 펜던트自定义매개변수页面及弹出的数字键盘](assets/api-13.png)

QtCreator 제어台会打印전송到컨트롤러的데이터

![Qt Creator 中封装매개변수并通过 sendMessage 전송 0x9201 명령어的代码和输出日志](assets/api-14.png)

![Qt Creator 中수신컨트롤러 0x9201 명령어的槽函数及 JSON 调试日志](assets/api-15.png)

如果호출函数 void hideTechnologyToolbuttons();会隐藏除프로세스在主界 面上自定义버튼以外的其他프로세스버튼

![티치 펜던트로봇제어软件인터페이스，포함프로세스导航、基本제어和监控面板](assets/api-16.png)

- Demo 파일类설명
