# 核心概念

## 主站开发流程

1. 使能实时环境 → `enableRealtimeEnvironment()`
2. 注册日志回调 → `registerCustomeAppLog()`
3. 启动主站 → `startEcMaster()`
4. 获取从站数量和 ID → `getSlaveIDVec()`
5. 获取 PDO 地址映射 → `getPDOAddrVec()`
6. 注册周期任务回调 → `registerCustomeAppWorkpd()`
7. 进入事件循环
