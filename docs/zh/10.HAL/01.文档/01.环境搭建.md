# 环境搭建

## 环境要求

| 项目 | 要求 |
|------|------|
| 操作系统 | Ubuntu 20.04 LTS |
| 系统架构 | x86_64 |
| 编译器 | GCC 9.4.0 / GCC 4.8.2 |
| 依赖库 | libpthread、librt、libdl、libm |

## 导入库文件

1. 将 `libhal_sdk.a` 静态库复制到项目的 `lib` 目录
2. 将 `include` 文件夹下的头文件复制到项目的 `include` 目录
3. 编译时链接 `libhal_sdk.a`
